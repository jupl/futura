{
  "ext": "ts",
  "execMap": {
    "ts": "node --inspect -r esm -r isomorphic-fetch -r reflect-metadata -r dotenv/config -r tsconfig-paths/register -r ts-node/register/type-check"
  },
  "ignore": [
    "**/components/**",
    "**/test.*",
    "**/*.test.*",
    "pages/**"
  ],
  "watch": [
    "src/**/*.ts"
  ]
}
